# TP05 · 工程接线与联调

> 把新文件接进 Xcode target、确认入口、整体联调、真机验证。收尾任务。
>
> 依赖：TP01–TP04（接线本身可在 TP02/03 产出后就先做，解剖图文件后补再接一次）。

## 1. Xcode target 接线（关键坑）

本工程是**非同步组工程**，新建/新增 .swift 文件**必须**用 xcodeproj 脚本接 target，不能只把文件丢进目录。详见记忆 `xcode-target-wiring`。

需接入的新文件（按实际产出增删）：
- `ExerciseRow.swift`（TP02）
- `ExerciseDetailView.swift`（TP03）
- `MuscleBodyView.swift` / `MuscleMapView.swift` / `BodyPaths.swift`（TP04）

若走 PocketSVG（TP04 路线 A）：在 Xcode 工程加 SPM 依赖 `https://github.com/pocketsvg/PocketSVG`，并 link 到 `HealthApp` target。

> 注意工程已知的两个编译坑（见记忆 `xcode-target-wiring`），接线后先 build 验证。

## 2. 入口确认

入口已存在：[ProfileView.swift](../../../HealthApp/Features/Profile/ProfileView.swift) 「我的 → 小工具 → 训练计划」push `TrainingPlanView`。重构不改入口路径，确认 push 后是新版主页即可。

## 3. 导航打通

- 主页动作卡 → `ExerciseDetailView`
- 主页解剖图卡 → `MuscleMapView`
- 解剖图页动作行 → `ExerciseDetailView`
- 统一用 `NavigationStack` / `NavigationLink`（与 App 现有导航一致）。

## 4. 联调清单

- [ ] 7 分类 + 类型筛选 + 搜索 + 男女切换全链路无崩溃。
- [ ] 解剖图选肌群 → 动作列表 → 详情，三页跳转闭环。
- [ ] 详情页发力图高亮与所选动作一致。
- [ ] 空态：无视频 / 无要点 / 空肌群 都不崩。
- [ ] 深浅色、动态字体下版式正常。

## 5. 真机验证

- 出包一律 **Release**（见记忆 `device-release-only`），不装 Debug。
- 真机走查三屏 + 跳转 + 解剖图点击命中区是否准确。

## 6. 收尾

- 删除/归档旧 4 部位残留代码与不再用的占位资源名。
- 更新 [docs/prd/训练计划设计.md](../../prd/训练计划设计.md) 状态为「已被动作库重构取代」，或指向本目录。
- 按需补 `THIRD-PARTY-NOTICES`（PocketSVG / body-highlighter MIT）。

## 产出

- 工程可编译、真机可用的完整训练计划动作库。
